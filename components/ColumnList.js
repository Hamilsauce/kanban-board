import { Component } from './Component.js';
export class ColumnList extends Component {
  constructor() {
    super('column-list', {
      templateName: 'column-list',
      elementProperties: {},
    });
    
  };
  
  // get prop() { return this.#prop };
  // set prop(v) { this.#prop = v };
}